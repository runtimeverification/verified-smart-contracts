requires "verification-lemmas.k"
requires "semantics-abstraction.k"

module {MODULE}-SPEC
  imports ETHEREUM-SIMULATION
  imports VERIFICATION-LEMMAS
  imports SEMANTICS-ABSTRACTION

{RULES}

endmodule
