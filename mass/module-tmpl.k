requires "abstract-semantics.k"
requires "verification.k"

module {MODULE}-{CONTRACT_ADDRESS_STR}-SPEC
  imports ETHEREUM-SIMULATION
  imports ABSTRACT-SEMANTICS
  imports VERIFICATION

{RULES}

endmodule
